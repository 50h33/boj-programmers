#include <bits/stdc++.h>
using namespace std;
string s; int cnt;
int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);
	while (cin >> s) {
        cnt++;
    }
    cout << cnt << "\n";
}